# Core dependency
numpy>=1.20

# Optional: only needed to read the legacy .xls subject registry (DEMO_DATA.xls).
# If omitted, pass ECG events explicitly via parse_csv(..., ecg_events=[...]).
xlrd>=2.0
