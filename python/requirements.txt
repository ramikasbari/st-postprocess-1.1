# Core dependency (CSV/ECHOPAC path + strain math)
numpy>=1.20

# Optional: read the legacy .xls subject registry (DEMO_DATA.xls).
# If omitted, pass ECG events explicitly via parse_csv(..., ecg_events=[...]).
xlrd>=2.0

# Optional: image (DICOM) path only.
#   opencv  -> mask -> endocardial-contour extraction (mask_to_endocardial_contour)
#   torch   -> EchoNet-Dynamic segmentation adapter (EchoNetSegmenter)
# The CSV path and all core strain math need none of these.
# opencv-python-headless>=4.5
# torch>=1.12
# torchvision>=0.13
